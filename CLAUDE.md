# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is not yet initialized with code — only a README currently exists. This file is a
placeholder and should be updated (build/lint/test commands, project layout, architecture) as soon
as the .NET solution and Blazor project(s) are scaffolded.

## Project

Company Employees Management — an internship project built by a team of 4 developers.

- Stack: Blazor (.NET / C#)
- No solution, projects, or dependencies exist yet in this repo.

## Working conventions

- This is a team project (4 devs) — avoid unrequested refactors or restructuring of code written by
  others; keep changes scoped to what's asked.
- Update this file once the solution structure exists: add the `dotnet build` / `dotnet test` /
  `dotnet run` commands actually used, the project layout (e.g. Blazor Server vs WebAssembly, API vs
  monolith), and any architectural patterns (layering, EF Core usage, auth) once they're established.
