# SQL scripts

- `reset-delegation-test-data.sql` resets delegation data used by tests.

Schema changes belong in Entity Framework migrations under `src/Backend/CompanyEmployees.Persistence/Migrations/`.
